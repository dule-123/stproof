🔍 实寄封数字存证 · 验证指南

本指南说明如何验证本仓库中每一枚实寄封的数字存证数据。验证过程不需要信任任何第三方，只需使用开源工具和公开数据即可完成。

验证目标

确认以下三项内容均未被篡改：

1. 原始照片（你本地保存的 JPG 文件）与链上记录一致
2. 时间戳（OTS 文件）证明照片在某个时间点之前已存在
3. JSON 描述与照片内容逻辑一致

所需工具

• sha256sum（Linux/macOS 自带，Windows 可用 certutil -hashfile 或 Git Bash 中的 sha256sum）

• ots（OpenTimestamps 客户端，https://github.com/opentimestamps/opentimestamps-client）

• 任意文本编辑器或 JSON 查看器

验证步骤

第一步：验证照片哈希

# 计算本地原始照片的 SHA256
sha256sum 正面.jpg
sha256sum 背面.jpg


将输出的哈希值与 sha256/ 目录中对应的 .sha256 文件内容进行比对。两者必须完全一致。

第二步：验证时间戳

# 验证照片的时间戳
ots verify 正面.jpg -o 正面.jpg.ots
ots verify 背面.jpg -o 背面.jpg.ots


如果输出 Success! 或类似提示，说明时间戳有效，该照片在时间戳记录的时间点之前已存在且未被篡改。

第三步：验证 JSON 描述

打开 manifests/ 目录中对应的 JSON 文件，检查其中的描述是否与照片内容一致：

• 邮票种类、面值是否匹配

• 邮戳日期、地点是否清晰可见

• 挂号条码是否吻合（注意：条码末几位可能已脱敏）

• 品相描述是否如实

第四步：验证 IPFS CID

如果你有 IPFS 节点，可以直接通过 CID 拉取存证文件：
ipfs get <CID>


对比拉取到的文件与本仓库中的文件是否一致。

验证流程示意图


本地原始照片
     │
     ├─→ 计算 SHA256 ──→ 比对 sha256/ 目录中的指纹文件
     │
     ├─→ 验证 OTS 时间戳 ──→ 确认照片在某个时间点前已存在
     │
     └─→ 目视比对 ──→ 确认 JSON 描述与照片内容一致
     
所有步骤通过 → 存证有效
任一步骤失败 → 存证可能已被篡改


注意事项

• 原始照片不上传：本仓库及 IPFS 中均不包含原始照片，仅包含哈希指纹和时间戳。原始照片请妥善保存在本地或冷备份介质中。

• 哈希值不可逆：从 SHA256 无法还原原始照片，因此即使哈希值公开，照片内容依然安全。

• OTS 依赖比特币区块链：OTS 时间戳的最终安全性依赖于比特币区块链的存续。建议定期（如每年）重新验证并更新时间戳。

• JSON 描述仅供参考：JSON 中的文字描述是人工编写的，不作为绝对证据。最终验证应以照片内容为准。

常见问题

Q：我没有原始照片，能验证吗？  
A：不能。原始照片是验证链条的起点。如果你不是照片持有者，你只能验证 JSON 文件的完整性，但无法确认照片与描述是否一致。

Q：OTS 验证失败怎么办？  
A：可能的原因：时间戳文件与照片不匹配、照片已被修改、OTS 客户端版本过旧。请检查文件配对是否正确，并更新 ots 客户端后重试。

Q：IPFS 上的文件与本仓库不一致怎么办？  
A：以 IPFS 上的文件为准，因为 IPFS 的 CID 是基于内容的，任何修改都会导致 CID 变化。本仓库可能因更新不及时而存在滞后。

如有其他问题，请通过 GitHub Issues 或 Discussions 联系。
