# 2026-15J 第48届世技赛纪念邮票 · 首日原地实寄封数字档案

## 项目概述
本仓库为 **第48届世界技能大赛（世技赛）主题首日原地实寄封** 的永久数字档案。
包含 01~10 号封（及公告文件）的实物影像、OTS 时间戳证明与元数据索引。

- **赛事背景**：第48届世技赛 / 深职大公函封 / 西丽邮政支局（原地）
- **实物特征**：同城挂号实寄，符资 3.80 元（鲁班+世赛+保护森林组合）
- **存证技术**：IPFS 永久存储 + OTS（OpenTimestamps）时间戳 + SHA-256 哈希

## 数字档案总入口（IPFS）
本系列的核心索引文件已上传至 IPFS，**以下 CID 为整个档案的永久数字指纹**：

🔗 **总索引 CID**：`bafkreigikk5udc7hvjoawmezaes6nqdbgehgmk72azvh56ymgvnsfs7rpa`
🌐 **网关访问**：`https://white-tired-rat-661.mypinata.cloud/ipfs/bafkreigikk5udc7hvjoawmezaes6nqdbgehgmk72azvh56ymgvnsfs7rpa`

## 文件结构与链路
完整证据链为：**GitHub 展示入口 → 总索引 JSON → 单封元数据 JSON → 原图 + OTS 证明**

- `index_STP-20260922.json`（总索引，CID 如上）
  - `01~10` 号封：各含 `metadata_*.json` → 指向 `.jpg` 原图 + `.ots` 时间戳
  - `公告文件`：独立元数据与存证

## 验证指引（如何自证真伪）
任何人可通过以下步骤验证本批实寄封的数字档案未被篡改：

1. **获取总索引**
   通过 IPFS 网关或本地 IPFS 节点拉取总索引：

2. **校验索引完整性**
计算下载的索引文件 SHA-256，应与仓库发布的校验值一致（可在本 README 下方核对）。

3. **逐层溯源**
打开 `index.json`，提取各封（01~10）的 `metadata JSON CID`，通过 IPFS 获取对应元数据，进而找到：
- 原图 CID（`.jpg`）
- 时间戳证明 CID（`.ots`）

4. **验证 OTS 时间戳**
使用 `ots` 工具验证原图的时间戳（确保实物影像在指定时间已存在）：
（需安装 OpenTimestamps 客户端）

5. **哈希比对**
本地计算原图 SHA-256，与 `metadata_*.json` 中记录的哈希值比对，完全一致即证明未篡改。

## 品相与实物说明
- **邮资**：同城挂号符资 3.80 元（鲁班1.20×2 + 世赛1.20 + 保护森林0.10×2）。
- **品相**：自然实寄状态，存在个别轻微漏销、红戳等自然瑕疵（详见各封 metadata）。
- **内信**：部分信封含内信（背面手写“有内信”），已影像存证。
- **坐标**：发件地为西丽邮政支局（原地），收件地为南油路2336号海滨小区（同城）。

## 归档信息
- **归档日期**：2026-09-22 / 2026-09-23（IPFS 固化）
- **总封数**：9枚（含公告文件共 11 项索引）
- **存证网络**：IPFS（Pinata 网关）+ OTS 区块链时间戳

---
🔒 本档案通过去中心化存储与时间戳技术，确保物理藏品与数字记录永久绑定、不可篡改。
